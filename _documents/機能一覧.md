# 機能一覧

Google Forms / Microsoft Forms 相当の機能をどこまで作るかの一覧。
方針は **できるだけフルカバー**（2026-08-18 決定）。

> **この一覧の出典について。** 両サービスの機能は当初**記憶に基づいて列挙**していたが、
> **2026-08-21 に公式ドキュメントで突き合わせた**（Issue #88。結果と出典は 9 章）。
> **製品側の機能は変わる。** 参照日から時間が経ったら引き直すこと。
> Pleasanter 側の制約についてはソースで確認済みで、根拠を各所に記載する。

## 第 1 弾の範囲（2026-08-18 決定）

**第 1 弾は ◎ のみ。** ○ と △ は後続に回す。

**◎ に条件分岐が入っているので、製品の価値（Pleasanter 純正では出来ないこと）は
第 1 弾で成立する。**

> **グリッド・ランキング・NPS・テーマ・多言語・下書き保存は実装済み**
> （2026-08-20、Issue #74 / #59 ほか）。
> 下の表の ◎ / ○ / △ は**当初の見立て**であって、今の実装状況ではない。

> **セキュリティと DDoS 対策は第 1 弾から作り込む**（2026-08-18 決定）。
> 機能一覧の ◎ / ○ の区分に関わらず、[`非機能設計.md`](非機能設計.md) 1 章・2 章の
> 対策は最初から入れる。**完全匿名の公開フォームを認証なしでインターネットへ晒す**以上、
> 後回しにできる部分が無い。

## 記号

| 記号 | 意味 |
|---|---|
| ◎ | 対応する |
| ○ | 対応する（後続でよい） |
| △ | 対応するが制約あり。備考を読むこと |
| × | 対象外。**後から追加できる作りにする** |
| — | Pleasanter 本体の機能に任せる |

## 1. 設問形式

| 機能 | 対応 | 備考 |
|---|---|---|
| 記述式（1 行） | ◎ | |
| 段落（長文） | ◎ | |
| ラジオボタン（単一選択） | ◎ | |
| チェックボックス（複数選択） | ◎ | 複数の `Check` 列へ展開する変換が要る（アーキテクチャ方針 8 章） |
| プルダウン | ◎ | |
| 直線尺度（1〜5 など） | ◎ | 数値列へ写す |
| 星評価 | ◎ | 数値列へ写す。表示だけの違い |
| 日付 | ◎ | **タイムゾーンに注意**（アーキテクチャ方針 6 章）。**未回答は `DateTime.MinValue` を送って未設定へ戻す**（実機検証結果 9 章。7 章 3.1） |
| 時刻 | ◎ | |
| 「その他」＋自由記述つき選択肢 | ◎ | 選択肢列＋自由記述列の 2 列へ展開する |
| 選択グリッド（行列・単一選択） | △ | **実装済み**（#74）。行ごとに 1 つの入力を出す。**1 設問が行数ぶんの列を食う** |
| チェックボックスグリッド（行列・複数選択） | △ | **実装済み**（#74）。同上。行ごとに複数選べる |
| ランキング（順位付け） | ○ | **実装済み**（#74）。項目ごとに順位を取り出せる。連結して 1 列にもできる。**Microsoft Forms の機能で、Google Forms には無い**（9.1） |
| ファイルのアップロード | ◎ | **`Upsert` に Base64 で同梱できる**（実機確認済み）。送信待ちに Base64 が載るためサイズ上限が要る |
| NPS（推奨度） | ○ | **実装済み**（#74）。**尺度のプリセット**（0〜10 ＋ 両端の文言）。専用の形式にはしない。**Microsoft Forms の機能で、Google Forms には無い**（9.1） |
| 説明文ブロック（設問ではない） | ◎ | **実装済み**（#108）。保存先の列を持たない要素。本文は記法で書け、太字・斜体・リンク・箇条書き・見出しが使える。**HTML は受け取らず、サーバが構造へ変換して保存する**（リンクは `https:` のみ） |
| 画像・動画の埋め込み | ○ | **実装済み**（Issue #104 / #107）。表示のみで保存先を持たない。`iframe` も差し込めるが、**配信元は `QUESTIONNAIRE_EMBED_ALLOWEDHOSTS` で絞る**（既定は空） |

## 2. 設問のオプション

| 機能 | 対応 | 備考 |
|---|---|---|
| 必須 | ◎ | |
| 説明文（設問の補足） | ◎ | |
| 既定値 | ◎ | |
| プレースホルダ | ◎ | |
| 入力検証（文字数） | ◎ | |
| 入力検証（数値の範囲） | ◎ | |
| 入力検証（メール形式・URL） | ◎ | |
| 入力検証（正規表現） | ○ | **実装済み**（#102）。**入力の全体が合うかを見る。** 後退戻りしない照合器（`RegexOptions.NonBacktracking`）＋ 100ms の上限で照合する。**その代わり先読み・後方参照・原子グループは使えず、公開時に弾く。** 合わないときの文言は設問ごとに指定でき、正規表現そのものは回答者へ見せない |
| 選択肢の数の下限・上限（複数選択） | ○ | **実装済み**（#101）。**下限は未回答には効かない**（答えないか、下限まで選ぶか）。答えさせるのは必須の役目。**下限が選択肢の数を超える指定は公開時に弾く** |
| 選択肢のシャッフル | ○ | **実装済み**（#103）。「その他」は入れ替えず末尾に固定する。**並び順は 1 回の回答の中で固定**（戻っても位置が変わらない） |
| 設問の順序のシャッフル | ○ | **実装済み**（#103）。**表示条件を持つ設問があるページでは指定できない**（公開時に弾く）。**説明文ブロックは動かさない**（前置きが説明する対象から離れるため） |

## 3. ページとフロー

| 機能 | 対応 | 備考 |
|---|---|---|
| ページ分割（セクション） | ◎ | |
| 前へ戻る | ◎ | |
| 進捗バー | ◎ | |
| 1 問 1 ページ表示 | ◎ | フォーム全体の表示モード |
| **回答に応じたページ分岐** | ◎ | **本ツールの中心機能。** Pleasanter 純正に無い。**Google はセクション単位、Microsoft は設問単位も可**（9.3） |
| 回答に応じた設問の出し分け | ◎ | 同上 |
| ページの並べ替え | ◎ | 管理アプリ側 |

## 4. フォーム全体

| 機能 | 対応 | 備考 |
|---|---|---|
| タイトル・説明 | ◎ | |
| 送信後の確認メッセージ | ◎ | |
| 「別の回答を送信」の導線 | ◎ | |
| 回答の編集を許可 | ◎ | GUID ＋ `Upsert`（アーキテクチャ方針 9 章） |
| 多重投稿の抑止 | △ | Cookie / Web Storage。**端末単位であり本人確認ではない。** ⚠️ **Microsoft Forms でも「1 人 1 回」は組織内限定のときだけ**（9.3）。完全匿名の公開フォームで確実に止める手段は、あちらにも無い |
| 受付の開始・終了日時 | ◎ | |
| 回答数の上限 | ○ | |
| 受付の手動停止 | ◎ | |
| 下書き保存・再開 | ○ | **実装済み**（#59）。**端末の中だけ**に置き、サーバへは送らない。**アンケートごとに切り替え、既定は無効。** ⚠️ **Google Forms は既定で有効・サーバ側に 30 日**（9.2）。完全匿名の前提では同じ方式を採れない |
| テーマ（色・フォント） | ○ | **実装済み**（#56 / #109）。名前の付いた配色 5 種から選べる。色は 6 本（強調・地・文字・釦の文字・入力欄の地・枠線）まで個別に指定でき、**読みにくい組み合わせは管理画面が警告する**（保存は止めない） |
| ヘッダ画像 | ○ | |
| 多言語 | ○ | 日本語と英語。データモデルも画面の文言も対応済み（[`多言語対応方針.md`](多言語対応方針.md)） |
| bot・スパム対策 | ◎ | **第 1 弾から入れる。** 完全匿名の公開フォームのため（[`非機能設計.md`](非機能設計.md) 1 章） |
| アクセス解析のタグ（GA4・GTM・Matomo・Plausible） | ○ | **実装済み**（Issue #162）。**回答画面だけ・既定は無効。** 受け取るのは ID と配信元だけで、任意のスクリプトは貼れない。**有効にしたときだけ CSP を広げる**。回答者への告知も出す（既定オン） |
| 回答のコピーを回答者へ送信（自動返信メール） | × | **今回は対象外**（2026-08-18 決定）。後から追加する可能性があるため、送信基盤を差し込める形にしておく |

## 5. 管理側

| 機能 | 対応 | 備考 |
|---|---|---|
| フォームの複製 | ◎ | |
| プレビュー | ◎ | |
| 公開 URL の発行 | ◎ | **`/f/{PublicId}`。** 推測不能な乱数で、サイト ID を URL に出さない |
| QR コード | ○ | |
| 未送信（一時保管）の状況表示 | ◎ | 件数と最古の滞留時刻（アーキテクチャ方針 10 章） |
| テンプレート | ○ | |
| 共同編集 | × | 対象外 |
| 回答の一覧・集計・グラフ | — | **Pleasanter の機能に任せる** |
| CSV エクスポート | — | 同上 |

## 6. スコープ全体に効く制約

### 列の本数

**1 設問が複数列を食う形式（グリッド・ランキング・「その他」つき選択肢）があるため、
設問数の上限は列の本数で決まる。**

- **標準は型ごとに 26 本**（`A`〜`Z`）。実機で確認済み（[`実機検証結果.md`](実機検証結果.md)）
- **項目拡張**で型ごとの本数を増やせる。CodeDefiner の `trial` 既定では
  `Class` に 100 本（`Class001`〜`Class100`）が追加され 126 本になった
- **`Class` 列は 1024 文字が上限。** 複数選択を連結して 1 列へ入れる方式は、
  選択肢が多いと溢れる
- **管理アプリで、残りの列数と超過を必ず表示すること。** 保存時に初めて失敗すると、
  作り直しになる

### 集計は Pleasanter に任せる

**本ツールは集計機能を作らない。** 回答は Pleasanter のレコードとして蓄積するので、
一覧・集計・グラフ・エクスポートは本体の機能を使う。
**この線を越えると、Pleasanter をバックエンドにする意味が薄れる。**

### 後から足せる形にしておくもの

- **自動返信メール** — 今回は作らないが、送信基盤を差し込める形にする
- **英語以外の言語** — 器も画面の文言も言語コードで引く形になっている
  （[`多言語対応方針.md`](多言語対応方針.md)）。言語を足すのはカタログを 1 本増やすこと
- **回答者の認証** — いまは完全匿名。厳密な一意性が要るようになったら
  URL トークン方式へ変える

## 7. 未確定

| # | 論点 | 状態 |
|---|---|---|
| 1 | この一覧を公式ドキュメントと突き合わせる（記憶ベースのため） | **決着**（2026-08-21。Issue #88。結果は 9 章） |

### 決着させた事項（2026-08-20）

| # | 論点 | 決定 |
|---|---|---|
| 3.1 | 任意回答の日付の「未回答」 | **`DateTime.MinValue`（`0001-01-01T00:00:00`）を送る。** Pleasanter 側は未設定へ戻る（実機で確認。実機検証結果 9 章）。⚠️ **一覧では `1899-12-30` に見える**が、**回答の正本 JSON には項目そのものが無い**ので、さかのぼれば区別できる |
| 4 | bot 対策の方式 | **外部の CAPTCHA サービスを使わない。** ALTCHA（自前設置の proof-of-work、MIT / BSD-2-Clause）。⚠️ **外部サービスは回答者の情報を第三者へ送るので、完全匿名と両立しない** |
| 5 | ファイルアップロードの投稿経路 | **送信待ちの行に Base64 で載せ、`Upsert` のボディへ同梱する**（実機確認済み） |

## 8. 第 1 弾に入らなかったもの（第 2 弾）

**2026-08-20 決定。** 今入っているものを第 1 弾とする。

| 機能 | なぜ後回しでよいか |
|---|---|
| ~~入力検証（正規表現）~~ | **実装済み**（#102）。文字数・数値の範囲・メール・URL は元から入っていた。実行時間の上限は、上限を付けるだけでなく**後退戻りしない照合器へ切り替えて**原理的に長引かないようにした |
| ~~選択肢の数の下限・上限（複数選択）~~ | **実装済み**（#101）。必須の有無で足りる場面が多いと見ていたが、先に入れた |
| ~~選択肢・設問の順序のシャッフル~~ | **実装済み**（#103）。分岐と併用できないのは設問の入れ替えだけで、**選択肢の入れ替えは併用できる**（選択肢ごとに行き先が付いているため）と分かった |
| 画像・動画の埋め込み | **実装済み**（Issue #104 / #107）。配信元は運用側の設定で絞る |
| 自動返信メール | **対象外**（2026-08-18 決定）。送信基盤を差し込める形にはしてある |

## 9. 公式ドキュメントとの突き合わせ（2026-08-21）

**Issue #88。** この一覧は記憶ベースで書かれていたため、公式ドキュメントで事実確認した。
**判断ではなく事実確認**であり、実装するかどうかはここでは決めない。

### 出典

**すべて 2026-08-21 参照。ブログ・第三者記事は根拠にしていない。**

| 記号 | 文書 | URL |
|---|---|---|
| G1 | Choose a type of question for your form（Google Forms） | <https://support.google.com/docs/answer/7322334> |
| G2 | Add, edit, or format text, images, or videos（Google Forms） | <https://support.google.com/docs/answer/2839737> |
| G3 | Publish and share your form（Google Forms） | <https://support.google.com/docs/answer/2839588> |
| G4 | Show questions based on answers（Google Forms） | <https://support.google.com/docs/answer/141062> |
| M1 | Create a form with Microsoft Forms | <https://support.microsoft.com/en-US/Forms/create-a-form-with-microsoft-forms> |
| M2 | Adjust your form or quiz settings in Microsoft Forms | <https://support.microsoft.com/en-US/Forms/adjust-your-form-or-quiz-settings-in-microsoft-forms> |
| M3 | Use branching logic in Microsoft Forms | <https://support.microsoft.com/en-US/Forms/use-branching-logic-in-microsoft-forms> |

### 9.1 設問形式

**✓ は公式ドキュメントに型として挙がっているもの。** 挙がっていない型は「—」とし、
**「無い」と断定はしない**（ページに書かれていないだけの場合がある）。

| 設問形式 | Google | Microsoft | 出典 |
|---|---|---|---|
| 記述式（短文） | ✓ Short answer | ✓ Text | G1 / M1 |
| 段落（長文） | ✓ Paragraph | ✓ Text（長文の設定） | G1 / M1 |
| ラジオボタン（単一選択） | ✓ Multiple choice | ✓ Choice | G1 / M1 |
| チェックボックス（複数選択） | ✓ Checkboxes | ✓ Choice（複数選択の設定） | G1 / M1 |
| プルダウン | ✓ Dropdown | — | G1 |
| 直線尺度 | ✓ Linear scale | ✓ Likert（尺度の行列） | G1 / M1 |
| 星評価 | ✓ Rating | ✓ Rating | G1 / M1 |
| 日付 | ✓ Date | ✓ Date | G1 / M1 |
| 時刻 | ✓ Time | — | G1 |
| 選択グリッド（行列・単一選択） | ✓ Multiple choice grid | ✓ Likert | G1 / M1 |
| チェックボックスグリッド（行列・複数選択） | ✓ Checkbox grid | — | G1 |
| ランキング（順位付け） | — | ✓ Ranking | M1 |
| NPS（推奨度） | — | ✓ Net Promoter Score | M1 |
| ファイルのアップロード | ✓ File upload | ✓ File upload | G1 / M1 |
| セクション（ページ分割） | ✓ Section | ✓ Section | G2 / M1 |

**⚠️ ランキングと NPS は Google Forms の公式ページに型として挙がっていない。**
一覧では両方を「Forms 相当」として扱っていたが、**Microsoft Forms 側の機能**である。

### 9.2 一覧に無かった機能

**公式ドキュメントで確認できた機能のうち、この一覧に項目が無かったもの。**
**実装の可否はここでは決めない。**

| 機能 | どちらに | 内容 | 出典 |
|---|---|---|---|
| **クイズ（自動採点）** | 両方 | 正解の設定、点数、正誤の即時表示 | G3 / M2 |
| **練習モード** | Microsoft | 提出前に正解を見ながら解ける | M2 |
| **回答の制限時間** | Microsoft | 時間切れで自動提出。回答者は途中で止められない | M2 |
| **回答のコピーを回答者が持ち帰る** | Microsoft | 完了画面から PDF の保存・印刷。組織内ならメールでも受け取れる | M2 |
| **回答ごとの管理者向けメール通知** | Microsoft | 1 件届くたびに知らせる | M2 |
| **設問番号を出さない設定** | Microsoft | 通し番号を隠す | M2 |
| **回答の下書きの自動保存** | Google | **既定で有効。最後の編集から 30 日**。切ることもできる | G2 |
| **事前入力リンク** | Google | 一部の項目を埋めた状態の URL を配る | G3 |
| **公開・非公開の切り替え** | Google | 未公開のフォームはリンクを知っていても開けない | G3 |

> **下書き保存は「あちらにもある」ことが分かった。** 本アプリは**端末の中だけ**に置く実装
> （Issue #59）で、Google は**サーバ側に 30 日**という違いがある。
> **回答者を完全匿名にする前提では、サーバへ置く方式は採れない。**

### 9.3 仕様の差（一次情報）

| 論点 | Google | Microsoft | 出典 |
|---|---|---|---|
| **分岐の単位** | **セクション単位**（回答に応じて次のセクションへ飛ばす） | **設問単位とセクション単位の両方** | G4 / M3 |
| 分岐の向き | 後ろへのみ | **後ろへのみ。** 前の設問へ戻すと以降が飛ばされて壊れる、と明記 | G4 / M3 |
| シャッフル | 設問・選択肢の両方 | 設問（**セクションがあると使えない**）。除外する設問を指定できる | G2 / M2 |
| 進捗バー | — | **セクションが複数あるときだけ**使える | M2 |
| 多重投稿の抑止 | — | **組織内限定のときだけ**「1 人 1 回」を選べる | M2 |
| 受付期間 | — | **開始・終了の日時**を指定できる | M2 |
| 規模の上限 | 設問・説明・画像などを合わせて **300 個**、セクション **75 個** | タイトル **90 文字**、説明 **1000 文字** | G2 / M1 |

### 9.4 突き合わせて分かった、この一覧の直し

- **ランキングと NPS を「Google にもある」前提で扱わない**（9.1）
- **下書き保存は Google にもある。** ただし**保存先が違う**（9.2）
- **分岐は Microsoft の方が細かい**（設問単位）。本アプリは設問単位の出し分けも持つので、
  **「Pleasanter 純正に無い」ことは変わらないが、「Forms に無い」わけではない**
- **多重投稿の抑止は、Microsoft でも組織内限定のときだけ。**
  完全匿名の公開フォームで確実に止める手段は、**あちらにも無い**

### 9.5 公式ドキュメントで確認できなかったもの

**推測で埋めない。** 次は今回の出典からは確認できなかった。

- **入力検証**（文字数・数値の範囲・メール形式・URL・正規表現）の詳細。
  Google の設問形式のページには検証の記載が無く、Microsoft 側も専用の記述を見つけられなかった
- **プレースホルダ**（両方）
- **1 問 1 ページ表示**（両方）
- **CAPTCHA・bot 対策**（両方）
- **回答数の上限**（両方）
- **QR コード・テンプレート・共同編集**の細部（存在は別ページにあるが、今回は引いていない）
- **Google のファイルアップロードで回答者に Google アカウントが要るか**
  （G1 に「サインイン」に触れる記述はあるが、条件を読み切れていない）

**必要になった時点で、上の出典から引き直すこと。**
