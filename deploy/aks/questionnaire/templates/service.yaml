apiVersion: v1
kind: Service
metadata:
  name: {{ include "questionnaire.fullname" . }}
  labels:
    {{- include "questionnaire.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "questionnaire.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
