# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: english.spec.ts >> 英語の画面 >> 英語を選ぶと日本語が残らない
- Location: specs/english.spec.ts:88:3

# Error details

```
Error: 英語の画面に日本語が残っている: 最初の管理者を登録する / まだ管理者が登録されていません。最初の 1 人を作ってください。 / この入口はここで一度きりです。 / ログイン ID / 合言葉 / 合言葉（確認） / 12 文字以上にしてください。 / 登録する

expect(received).toEqual(expected) // deep equality

- Expected  -  1
+ Received  + 10

- Array []
+ Array [
+   "最初の管理者を登録する",
+   "まだ管理者が登録されていません。最初の 1 人を作ってください。",
+   "この入口はここで一度きりです。",
+   "ログイン ID",
+   "合言葉",
+   "合言葉（確認）",
+   "12 文字以上にしてください。",
+   "登録する",
+ ]
```

# Page snapshot

```yaml
- generic [ref=e4]:
  - heading "最初の管理者を登録する" [level=1] [ref=e5]
  - paragraph [ref=e6]:
    - text: まだ管理者が登録されていません。最初の 1 人を作ってください。
    - strong [ref=e7]: この入口はここで一度きりです。
  - generic [ref=e8]:
    - generic [ref=e9]:
      - text: ログイン ID
      - textbox "ログイン ID" [ref=e10]
    - generic [ref=e11]:
      - text: 合言葉
      - textbox "合言葉" [ref=e12]
    - generic [ref=e13]:
      - text: 合言葉（確認）
      - textbox "合言葉（確認）" [ref=e14]
    - paragraph [ref=e15]: 12 文字以上にしてください。
    - button "登録する" [ref=e16] [cursor=pointer]
```

# Test source

```ts
  12  | 
  13  | /** 見に行く画面。**回答画面は公開が要るので、ここでは管理画面だけ見る。** */
  14  | const screens = [
  15  |   { name: 'admin-setup', path: '/admin?lang=en' },
  16  |   { name: 'admin-root', path: '/admin' },
  17  | ];
  18  | 
  19  | test.describe.configure({ mode: 'serial' });
  20  | 
  21  | test.describe('英語の画面', () => {
  22  |   for (const screen of screens) {
  23  |     test(`${screen.name}：横にはみ出さない`, async ({ page }) => {
  24  |       await page.goto(screen.path);
  25  |       await page.waitForLoadState('networkidle');
  26  | 
  27  |       const overflow = await page.evaluate(() => {
  28  |         const results: { text: string; overflowBy: number }[] = [];
  29  |         const viewportWidth = document.documentElement.clientWidth;
  30  | 
  31  |         for (const element of document.querySelectorAll('button, label, h1, h2, p, a, li')) {
  32  |           const rect = element.getBoundingClientRect();
  33  |           if (rect.width === 0) {
  34  |             continue;
  35  |           }
  36  | 
  37  |           // **画面の右端を越えているものを拾う**
  38  |           if (rect.right > viewportWidth + 1) {
  39  |             results.push({
  40  |               text: (element.textContent ?? '').trim().slice(0, 40),
  41  |               overflowBy: Math.round(rect.right - viewportWidth),
  42  |             });
  43  |           }
  44  |         }
  45  | 
  46  |         return results;
  47  |       });
  48  | 
  49  |       expect(
  50  |         overflow,
  51  |         `画面の右端からはみ出している: ${overflow.map((o) => `「${o.text}」(+${o.overflowBy}px)`).join(', ')}`,
  52  |       ).toEqual([]);
  53  |     });
  54  | 
  55  |     test(`${screen.name}：文字が入れ物からあふれない`, async ({ page }) => {
  56  |       await page.goto(screen.path);
  57  |       await page.waitForLoadState('networkidle');
  58  | 
  59  |       const clipped = await page.evaluate(() => {
  60  |         const results: { text: string; by: number }[] = [];
  61  | 
  62  |         for (const element of document.querySelectorAll('button, label')) {
  63  |           const style = getComputedStyle(element);
  64  | 
  65  |           // **隠す指定がある所だけを見る。** 折り返す所は伸びるだけで問題ない
  66  |           if (style.overflow === 'visible' && style.textOverflow !== 'ellipsis') {
  67  |             continue;
  68  |           }
  69  | 
  70  |           if (element.scrollWidth > element.clientWidth + 1) {
  71  |             results.push({
  72  |               text: (element.textContent ?? '').trim().slice(0, 40),
  73  |               by: element.scrollWidth - element.clientWidth,
  74  |             });
  75  |           }
  76  |         }
  77  | 
  78  |         return results;
  79  |       });
  80  | 
  81  |       expect(
  82  |         clipped,
  83  |         `文字が切れている: ${clipped.map((c) => `「${c.text}」(+${c.by}px)`).join(', ')}`,
  84  |       ).toEqual([]);
  85  |     });
  86  |   }
  87  | 
  88  |   test('英語を選ぶと日本語が残らない', async ({ page }) => {
  89  |     await page.goto('/admin?lang=en');
  90  |     await page.waitForLoadState('networkidle');
  91  | 
  92  |     // **訳し忘れがあると日本語のまま出る。** 画面に日本語が 1 文字でも残っていたら拾う
  93  |     const japanese = await page.evaluate(() => {
  94  |       const found: string[] = [];
  95  |       const pattern = /[぀-ゟ゠-ヿ一-鿿]/;
  96  | 
  97  |       const walker = document.createTreeWalker(document.body, NodeFilter.SHOW_TEXT);
  98  |       let node = walker.nextNode();
  99  | 
  100 |       while (node !== null) {
  101 |         const text = (node.textContent ?? '').trim();
  102 |         if (text !== '' && pattern.test(text)) {
  103 |           found.push(text.slice(0, 40));
  104 |         }
  105 | 
  106 |         node = walker.nextNode();
  107 |       }
  108 | 
  109 |       return found;
  110 |     });
  111 | 
> 112 |     expect(japanese, `英語の画面に日本語が残っている: ${japanese.join(' / ')}`).toEqual([]);
      |                                                                  ^ Error: 英語の画面に日本語が残っている: 最初の管理者を登録する / まだ管理者が登録されていません。最初の 1 人を作ってください。 / この入口はここで一度きりです。 / ログイン ID / 合言葉 / 合言葉（確認） / 12 文字以上にしてください。 / 登録する
  113 |   });
  114 | });
  115 | 
```